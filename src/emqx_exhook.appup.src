%% -*-: erlang -*-
{"e4.2.1",
  [
    {"e4.2.0", [
      {restart_application, emqx_exhook},
      {apply, {emqx_plugins, load, []}},
      {apply, {emqx_modules, refresh_module, [exhook]}}

    ]}
  ],
  [
    {"e4.2.0", [
      {restart_application, emqx_exhook},
      {apply, {emqx_plugins, load, []}}
    ]}
  ]
}.
